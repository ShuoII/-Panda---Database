game.StarterGui:SetCore("SendNotification", {
Title = "Panda-Guard Exception Handler"; -- the title (ofc)
Text = "The Script you Executed is current patched or unavailable, - Security at it's finest"; -- what the text says (ofc)
Duration = 10; -- how long the notification should in secounds
})
